node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Claude Code (machine-local)
.claude/settings.local.json

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# OpenTofu / Terraform
terraform/.terraform/
terraform/*.tfplan
terraform/terraform.tfvars
terraform/*.auto.tfvars
crash.log
crash.*.log
# The state file (terraform/terraform.tfstate) IS committed on purpose — it is
# encrypted at rest (see terraform/encryption.tf). NEVER disable that encryption.
# Backups and lock-info may contain plaintext, so keep them out:
terraform/*.tfstate.*
# Keep .terraform.lock.hcl tracked.
