node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Claude Code (machine-local)
.claude/settings.local.json

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# OpenTofu / Terraform
terraform/.terraform/
terraform/*.tfplan
terraform/terraform.tfvars
terraform/*.auto.tfvars
crash.log
crash.*.log
# State lives in Cloudflare R2 (see terraform/backend.tf), never in the repo.
terraform/*.tfstate
terraform/*.tfstate.*
# Keep .terraform.lock.hcl tracked.
