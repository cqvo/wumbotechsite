node_modules

# Astro build output
dist/
.astro/

# Pagefind search index (generated into public/ by `pnpm build`)
public/pagefind

# Deploy platforms
.vercel
.netlify
.wrangler

# OS
.DS_Store
Thumbs.db

# Claude Code (machine-local)
.claude/settings.local.json
.claude/worktrees/

# Env
.env
.env.*
!.env.example
!.env.test

# OpenTofu / Terraform
terraform/.terraform/
terraform/*.tfplan
terraform/terraform.tfvars
terraform/*.auto.tfvars
crash.log
crash.*.log
# State lives in Cloudflare R2 (see terraform/backend.tf), never in the repo.
terraform/*.tfstate
terraform/*.tfstate.*
# Keep .terraform.lock.hcl tracked.
